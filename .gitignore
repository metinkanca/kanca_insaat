# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-admin
release
release-updates
dist-ssr
*.local

# Secrets — never commit
.env
serviceAccountKey.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Firebase CLI local state / project mapping (contains the admin hosting site name)
.firebase
.firebaserc
electron/admin-target.json

# Local Claude Code settings
.claude/settings.local.json

# Test / import spreadsheets (may hold real product & price data)
*.xlsx
*.xls

# Build artifacts
*.exe

# Local project documentation (kept out of the public repo)
README.md
