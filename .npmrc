# Cloudflare's build environment sets NODE_ENV=production, which makes npm skip
# devDependencies. Every build tool this project needs (vite, typescript, react,
# firebase) lives there deliberately, so electron-builder keeps them out of the
# desktop installer's asar. Without this line the hosted build fails with
# "'tsc' is not recognized" before it compiles anything.
include=dev
